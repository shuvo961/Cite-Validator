{
  "name": "cite-validator",
  "version": "0.1.0",
  "private": true,
  "description": "Free Cite Validator app for reference validation, citation checking, and hallucinated citation detection.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
